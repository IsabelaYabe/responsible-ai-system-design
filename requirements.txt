# Anti-spoiler reading assistant — runtime deps.
# Local: `conda create -n antispoiler python=3.11 && pip install -r requirements.txt`
# Colab: the notebook pip-installs these in its first cell.

anthropic>=0.40
openai>=1.40
sentence-transformers>=3.0
faiss-cpu>=1.8
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3        # Cohen's kappa for judge validation
python-dotenv>=1.0       # local .env credential fallback
nltk>=3.8                # WordNet dictionary grounding for the definition validator
torch>=2.0               # Edit Predictor training, inference, and dataset artifacts
transformers>=4.40       # Hugging Face tokenizers/models for explanation module
spacy>=3.7               # lemma matching for CEFR pseudo-label generation (needs `python -m spacy download en_core_web_sm`)
matplotlib>=3.8          # training-curve plots in notebooks/explanation/train_edit_predictor.ipynb

# Interactive demo (app.py) — not needed for the eval/notebook.
fastapi>=0.110
uvicorn>=0.29
